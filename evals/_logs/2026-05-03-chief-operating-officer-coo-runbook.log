=== prompt ===
Use the chief-operating-officer agent: Create a runbook for restarting the whatsapp-bridge Go binary when it disconnects.

=== rc ===
0

=== duration_ms ===
137789

=== stdout ===
The runbook has been written to `/Users/winfredquek/crestbrick-consult/docs/runbook-whatsapp-bridge.md`. Here's what it covers:

- **Detection** — `pgrep` check + deliberate POST to `/api/send` (400 = alive, connection refused = dead) + log grep for `Connected to WhatsApp`
- **Manual restart** — step-by-step `launchctl` unload/load commands
- **launchd plist** — full template with `KeepAlive true` and 30-second throttle, so it can be recreated from scratch if lost
- **QR re-pairing** — exact sequence for when the session is invalidated (unload → wipe store → run interactively → scan → reload)
- **Common failure modes** — includes the 405/client-outdated pattern that required a rebuild in April 2026
- **Escalation table** — covers account bans, linked-device quota exhaustion, and the manual fallback for urgent client comms while the bridge is down


=== stderr ===


=== prompt ===
Use the chief-operating-officer agent: We are adding 2 new automation jobs (price-watcher, listing-scraper). Do we have launchd capacity and where should they live?

=== rc ===
0

=== duration_ms ===
116010

=== stdout ===
Here's the COO's operational brief:

---

**Capacity verdict: Yes — clear headroom.** You have 294 active launchd plists (not 372; that figure includes scripts, DB entries, and pipeline steps). macOS has no hard limit below ~1,000, so two more is a non-event.

---

**Where they should live:**

| Item | Path |
|---|---|
| Scripts | `~/.claude/bin/price-watcher.sh` and `~/.claude/bin/listing-scraper.sh` |
| Plists | `~/Library/LaunchAgents/com.crestbrick.price-watcher.plist` and `com.crestbrick.listing-scraper.plist` |
| Logs | `~/.claude/bin/price-watcher.launchd.{out,err}.log` (same pattern as existing jobs) |

**Scheduling:**
- **price-watcher** → `StartInterval 1800` (every 30 min) — portal data doesn't update faster
- **listing-scraper** → `StartCalendarInterval` at **07:45** and **14:15** SGT — avoids the congested 09:xx window where 4 other jobs already fire

---

**Main flag — functional overlap with 9 existing jobs:**

Before building, check if these are truly distinct from what's already running:
- `price-watcher` vs `watchlist-price-alert`, `price-reduction-monitor`, `hdb-price-alert`
- `listing-scraper` vs `competitor-listing-tracker`, `wa-group-listing-scraper`, `expired-listing-harvester`

If the distinction is a new **data source** (e.g., price-watcher pulls URA REALIS while existing jobs pull portals), make that explicit in the script name. If there's no real distinction, fold the logic into an existing script rather than adding new plists.


=== stderr ===


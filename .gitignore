.vercel

# Node
node_modules/

# Claude Code worktrees + agent state (local only)
.claude/
.claude-flow/
.swarm/
.mcp.json
.gitleaksignore

# Sensitive / client-private (review before un-ignoring)
client-deliverables/
hiring/

# Local agent/vector DBs (never commit)
agentdb.rvf
agentdb.rvf.lock
ruvector.db
*.rvf

# OS / editor cruft
.DS_Store
*.swp
.env*.local
.archive/
*.env
*.env.local
.telegram-bot.env
*credentials*.json
*-secrets.*
*.key
*.pem
_api_archive/

# Local-only PII tools (matchmaker app) — never commit
_local/

# Private databases + local state — NEVER commit (PII: tenants/landlords)
_templates/*-db.json
_templates/*-db.json.*
_templates/tenant-db.json*
_templates/landlord-db.json*
_templates/area-demand.json
_templates/buyer-intake.md
# These carry real landlord and tenant names, budgets and statuses
_templates/match-board.json
_templates/tenant-location-map.json
_local/
*.bak-*
.mcp.json.bak*
# Generated by scripts/build_*.py and carry real landlord and tenant names.
# Untracked 2026-08-10. Regenerated locally; nothing reads them as input.
docs/match-board.csv
docs/tenant-location-map.csv
docs/area-demand.csv
docs/landlord-database.csv
docs/tenant-database.csv
docs/*database*.csv
scripts/matchmaker/matchmaker-data*.json
scripts/matchmaker/deploy/index.html
scripts/patch_tenant_nightly*.py
__pycache__/

# Internal landlord working reports: real names, WhatsApp identifiers and
# verbatim conversation quotes. Generated locally, never committed.
docs/landlord-viewing-schedules.csv
docs/landlord-viewing-schedules.json
docs/landlord-viewing-schedules.md
docs/landlord-viewing-action-plan.md
docs/landlords-pending-extraction.md
docs/landlord-extraction-report.md

# Compiled bytecode: never source, and it embeds string literals
*.pyc

# Dated tenant working reports: real names and phone numbers.
docs/tenant-recommendations-*.md

# Matchmaker harvested-photo index (points at gitignored landlord photos)
scripts/matchmaker/photos-harvested.json

# Owner-sent photos Winfred hand-adds (PII, behind login only)
scripts/matchmaker/manual-photos/

# Public site build output (room photos, regenerated)
scripts/publicsite/dist/

# Root-level DB exports carry real names/phones — never commit (git add -A guard)
/landlord-db.json
/landlord-db.csv
/tenant-db.json
/tenant-db.csv
